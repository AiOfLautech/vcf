<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard | Contact Gain</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #6366f1;
      --primary-light: #818cf8;
      --secondary: #10b981;
      --danger: #ef4444;
      --warning: #f59e0b;
      --info: #0ea5e9;
      --dark: #1e293b;
      --light: #f8fafc;
      --gray: #94a3b8;
      --card-bg: #ffffff;
      --card-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Inter', sans-serif;
    }

    body {
      background-color: #f1f5f9;
      color: #334155;
      min-height: 100vh;
    }

    .dashboard-container {
      display: flex;
      min-height: 100vh;
    }

    /* Sidebar Styles */
    .sidebar {
      width: 260px;
      background: var(--dark);
      color: white;
      padding: 1.5rem 1rem;
      display: flex;
      flex-direction: column;
      box-shadow: var(--card-shadow);
      z-index: 10;
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 2rem;
      padding-bottom: 1rem;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    .logo i {
      font-size: 1.8rem;
      color: var(--primary-light);
    }

    .logo-text {
      font-size: 1.4rem;
      font-weight: 700;
    }

    .logo-text span:first-child {
      color: var(--primary-light);
    }

    .logo-text span:last-child {
      color: white;
    }

    .nav-links {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
      flex-grow: 1;
    }

    .nav-link {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 0.8rem 1rem;
      border-radius: 8px;
      text-decoration: none;
      color: var(--gray);
      transition: all 0.3s ease;
      font-weight: 500;
    }

    .nav-link:hover, .nav-link.active {
      background: rgba(99, 102, 241, 0.15);
      color: var(--primary-light);
    }

    .nav-link i {
      font-size: 1.2rem;
      width: 24px;
      text-align: center;
    }

    .admin-section {
      margin-top: auto;
      padding-top: 1rem;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
    }

    .admin-info {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .admin-avatar {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background: var(--primary);
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 700;
      color: white;
    }

    .admin-details {
      flex-grow: 1;
    }

    .admin-name {
      font-weight: 600;
      margin-bottom: 0.2rem;
      font-size: 0.95rem;
    }

    .admin-role {
      font-size: 0.8rem;
      color: var(--gray);
    }

    .logout-btn {
      display: block;
      width: 100%;
      text-align: center;
      padding: 0.6rem;
      margin-top: 1rem;
      background: rgba(239, 68, 68, 0.15);
      color: var(--danger);
      border-radius: 8px;
      text-decoration: none;
      transition: all 0.3s ease;
      font-size: 0.9rem;
      font-weight: 500;
    }

    .logout-btn:hover {
      background: rgba(239, 68, 68, 0.25);
    }

    /* Main Content Styles */
    .main-content {
      flex-grow: 1;
      padding: 2rem;
      overflow-y: auto;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
    }

    .header h1 {
      font-size: 1.8rem;
      font-weight: 700;
      color: var(--dark);
    }

    .header-actions {
      display: flex;
      gap: 1rem;
    }

    .header-btn {
      padding: 0.6rem 1rem;
      background: var(--card-bg);
      border: 1px solid #e2e8f0;
      border-radius: 8px;
      color: var(--dark);
      font-weight: 500;
      cursor: pointer;
      transition: all 0.3s ease;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      box-shadow: var(--card-shadow);
    }

    .header-btn:hover {
      background: #f8fafc;
      transform: translateY(-2px);
    }

    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 1.5rem;
      margin-bottom: 2.5rem;
    }

    .stat-card {
      background: var(--card-bg);
      border-radius: 12px;
      padding: 1.5rem;
      box-shadow: var(--card-shadow);
      transition: all 0.3s ease;
      border-left: 4px solid var(--primary);
    }

    .stat-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    }

    .stat-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 1rem;
    }

    .stat-title {
      font-size: 1rem;
      color: var(--gray);
      font-weight: 500;
    }

    .stat-icon {
      width: 40px;
      height: 40px;
      border-radius: 10px;
      background: rgba(99, 102, 241, 0.1);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.2rem;
      color: var(--primary);
    }

    .stat-value {
      font-size: 1.8rem;
      font-weight: 700;
      color: var(--dark);
      margin-bottom: 0.25rem;
    }

    .stat-change {
      font-size: 0.85rem;
      display: flex;
      align-items: center;
      gap: 0.3rem;
      color: var(--primary);
    }

    /* Charts Section */
    .grid-2-col {
      display: grid;
      grid-template-columns: 2fr 1fr;
      gap: 1.5rem;
      margin-bottom: 2.5rem;
    }

    .card {
      background: var(--card-bg);
      border-radius: 12px;
      padding: 1.5rem;
      box-shadow: var(--card-shadow);
    }

    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 1.5rem;
    }

    .card-title {
      font-size: 1.2rem;
      font-weight: 600;
      color: var(--dark);
    }

    .card-actions {
      display: flex;
      gap: 0.5rem;
    }

    .card-action {
      background: #f1f5f9;
      border: none;
      width: 32px;
      height: 32px;
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--gray);
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .card-action:hover {
      background: #e2e8f0;
      color: var(--dark);
    }

    .activity-list {
      list-style: none;
    }

    .activity-item {
      display: flex;
      gap: 1rem;
      padding: 1rem 0;
      border-bottom: 1px solid #e2e8f0;
    }

    .activity-item:last-child {
      border-bottom: none;
    }

    .activity-icon {
      width: 40px;
      height: 40px;
      border-radius: 10px;
      background: rgba(99, 102, 241, 0.1);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1rem;
      color: var(--primary);
      flex-shrink: 0;
    }

    .activity-details {
      flex-grow: 1;
    }

    .activity-title {
      font-weight: 500;
      margin-bottom: 0.25rem;
    }

    .activity-meta {
      font-size: 0.85rem;
      color: var(--gray);
      display: flex;
      gap: 1rem;
    }

    .activity-time {
      display: flex;
      align-items: center;
      gap: 0.25rem;
    }

    /* Responsive Styles */
    @media (max-width: 992px) {
      .sidebar {
        width: 220px;
      }
      
      .grid-2-col {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 768px) {
      .dashboard-container {
        flex-direction: column;
      }
      
      .sidebar {
        width: 100%;
        padding: 1rem;
      }
      
      .main-content {
        padding: 1.5rem;
      }
      
      .stats-grid {
        grid-template-columns: 1fr 1fr;
      }
    }

    @media (max-width: 576px) {
      .stats-grid {
        grid-template-columns: 1fr;
      }
      
      .header {
        flex-direction: column;
        align-items: flex-start;
        gap: 1rem;
      }
      
      .header-actions {
        width: 100%;
        justify-content: space-between;
      }
    }

    .activity-meta form button {
      background: none;
      border: none;
      cursor: pointer;
      font-weight: 500;
      padding: 0 8px;
    }
  </style>
</head>
<body>
  <div class="dashboard-container">
    <!-- Sidebar -->
    <div class="sidebar">
      <div class="logo">
        <i class="fas fa-chart-network"></i>
        <div class="logo-text">
          <span>Contact</span><span>Gain</span>
        </div>
      </div>
      <div class="nav-links">
        <a href="#" class="nav-link active"><i class="fas fa-chart-pie"></i> Dashboard</a>
        <a href="#" class="nav-link"><i class="fas fa-users"></i> Users</a>
        <a href="#" class="nav-link"><i class="fas fa-file-contract"></i> Sessions</a>
        <a href="#" class="nav-link"><i class="fas fa-download"></i> Downloads</a>
        <a href="#" class="nav-link"><i class="fas fa-cog"></i> Settings</a>
      </div>
      <div class="admin-section">
        <div class="admin-info">
          <div class="admin-avatar"><i class="fas fa-user-shield"></i></div>
          <div class="admin-details">
            <div class="admin-name">Admin</div>
            <div class="admin-role">System Administrator</div>
          </div>
        </div>
        <a href="/logout" class="logout-btn"><i class="fas fa-sign-out-alt"></i> Logout</a>
      </div>
    </div>

    <!-- Main Content -->
    <div class="main-content">
      <div class="header">
        <h1>Admin Dashboard</h1>
        <div class="header-actions">
          <button class="header-btn"><i class="fas fa-sync"></i> Refresh</button>
          <button class="header-btn"><i class="fas fa-download"></i> Export</button>
        </div>
      </div>

      <!-- Stats Grid -->
      <div class="stats-grid">
        <div class="stat-card">
          <div class="stat-header">
            <div class="stat-title">Total Users</div>
            <div class="stat-icon">
              <i class="fas fa-users"></i>
            </div>
          </div>
          <div class="stat-value"><%= stats.totalUsers %></div>
          <div class="stat-change">
            <i class="fas fa-arrow-up"></i> 12.4% from last week
          </div>
        </div>

        <div class="stat-card">
          <div class="stat-header">
            <div class="stat-title">Total Sessions</div>
            <div class="stat-icon">
              <i class="fas fa-calendar"></i>
            </div>
          </div>
          <div class="stat-value"><%= stats.totalSessions %></div>
          <div class="stat-change">
            <i class="fas fa-arrow-up"></i> 8.2% from last week
          </div>
        </div>

        <div class="stat-card">
          <div class="stat-header">
            <div class="stat-title">Active Sessions</div>
            <div class="stat-icon">
              <i class="fas fa-bolt"></i>
            </div>
          </div>
          <div class="stat-value"><%= stats.activeSessions %></div>
          <div class="stat-change">
            <i class="fas fa-arrow-up"></i> 5.3% from last week
          </div>
        </div>

        <div class="stat-card">
          <div class="stat-header">
            <div class="stat-title">Total Contacts</div>
            <div class="stat-icon">
              <i class="fas fa-address-book"></i>
            </div>
          </div>
          <div class="stat-value"><%= stats.totalContacts %></div>
          <div class="stat-change">
            <i class="fas fa-arrow-up"></i> 18.7% from last week
          </div>
        </div>

        <div class="stat-card">
          <div class="stat-header">
            <div class="stat-title">Total Downloads</div>
            <div class="stat-icon">
              <i class="fas fa-download"></i>
            </div>
          </div>
          <div class="stat-value"><%= stats.totalDownloads %></div>
          <div class="stat-change">
            <i class="fas fa-arrow-up"></i> 7.9% from last week
          </div>
        </div>

        <div class="stat-card">
          <div class="stat-header">
            <div class="stat-title">Succeeded Downloads</div>
            <div class="stat-icon">
              <i class="fas fa-check-circle"></i>
            </div>
          </div>
          <div class="stat-value"><%= stats.succeededDownloads %></div>
          <div class="stat-change">
            <i class="fas fa-arrow-up"></i> 9.2% from last week
          </div>
        </div>

        <div class="stat-card">
          <div class="stat-header">
            <div class="stat-title">Failed Downloads</div>
            <div class="stat-icon">
              <i class="fas fa-times-circle"></i>
            </div>
          </div>
          <div class="stat-value"><%= stats.failedDownloads %></div>
          <div class="stat-change">
            <i class="fas fa-arrow-down"></i> 3.1% from last week
          </div>
        </div>

        <div class="stat-card">
          <div class="stat-header">
            <div class="stat-title">Expired/Deleted Sessions</div>
            <div class="stat-icon">
              <i class="fas fa-trash"></i>
            </div>
          </div>
          <div class="stat-value"><%= stats.expiredOrDeletedSessions %></div>
          <div class="stat-change">
            <i class="fas fa-arrow-up"></i> 4.5% from last week
          </div>
        </div>

        <div class="stat-card">
          <div class="stat-header">
            <div class="stat-title">WhatsApp Links Added</div>
            <div class="stat-icon">
              <i class="fab fa-whatsapp"></i>
            </div>
          </div>
          <div class="stat-value"><%= stats.sessionsWithWhatsapp %></div>
          <div class="stat-change">
            <i class="fas fa-arrow-up"></i> 11.3% from last week
          </div>
        </div>
      </div>

      <!-- Manage Users Section -->
      <div class="card">
        <div class="card-header">
          <div class="card-title">Manage Users</div>
          <div class="card-actions">
            <button class="card-action"><i class="fas fa-ellipsis-h"></i></button>
          </div>
        </div>
        <ul class="activity-list">
          <% users.forEach(user => { %>
            <li class="activity-item">
              <div class="activity-icon"><i class="fas fa-user"></i></div>
              <div class="activity-details">
                <div class="activity-title"><%= user.username %></div>
                <div class="activity-meta">
                  <form action="/admin/delete-user/<%= user._id %>" method="POST" style="display:inline;">
                    <button type="submit" style="color: #ef4444;">Delete</button>
                  </form>
                  <form action="/admin/suspend-user/<%= user._id %>" method="POST" style="display:inline;">
                    <button type="submit" style="color: #f59e0b;">Suspend</button>
                  </form>
                  <form action="/admin/ban-user/<%= user._id %>" method="POST" style="display:inline;">
                    <button type="submit" style="color: #ef4444;">Ban</button>
                  </form>
                </div>
              </div>
            </li>
          <% }); %>
        </ul>
      </div>

      <!-- Manage Sessions Section -->
      <div class="card">
        <div class="card-header">
          <div class="card-title">Manage Sessions</div>
          <div class="card-actions">
            <button class="card-action"><i class="fas fa-ellipsis-h"></i></button>
          </div>
        </div>
        <ul class="activity-list">
          <% sessions.forEach(session => { %>
            <li class="activity-item">
              <div class="activity-icon"><i class="fas fa-calendar"></i></div>
              <div class="activity-details">
                <div class="activity-title"><%= session.groupName %></div>
                <div class="activity-meta">
                  <span>ID: <%= session.sessionId %></span>
                  <form action="/admin/delete-session/<%= session.sessionId %>" method="POST" style="display:inline;">
                    <button type="submit" style="color: #ef4444;">Delete</button>
                  </form>
                </div>
              </div>
            </li>
          <% }); %>
        </ul>
      </div>

      <!-- Recent Sessions and Downloads -->
      <div class="grid-2-col">
        <div class="card">
          <div class="card-header">
            <div class="card-title">Recent Sessions</div>
            <div class="card-actions">
              <button class="card-action"><i class="fas fa-ellipsis-h"></i></button>
            </div>
          </div>
          <ul class="activity-list">
            <% recentSessions.forEach(session => { %>
              <li class="activity-item">
                <div class="activity-icon"><i class="fas fa-calendar-plus"></i></div>
                <div class="activity-details">
                  <div class="activity-title"><%= session.groupName %> session created</div>
                  <div class="activity-meta">
                    <span>ID: <%= session.sessionId %></span>
                    <span class="activity-time"><i class="far fa-clock"></i> <%= session.createdAt.toLocaleDateString() %></span>
                  </div>
                </div>
              </li>
            <% }); %>
          </ul>
        </div>
        <div class="card">
          <div class="card-header">
            <div class="card-title">Recent Downloads</div>
            <div class="card-actions">
              <button class="card-action"><i class="fas fa-ellipsis-h"></i></button>
            </div>
          </div>
          <ul class="activity-list">
            <% recentDownloads.forEach(download => { %>
              <li class="activity-item">
                <div class="activity-icon" style="background: <%= download.status === 'success' ? 'rgba(16, 185, 129, 0.1)' : 'rgba(239, 68, 68, 0.1)' %>; color: <%= download.status === 'success' ? '#10b981' : '#ef4444' %>;">
                  <i class="fas <%= download.status === 'success' ? 'fa-check' : 'fa-times' %>"></i>
                </div>
                <div class="activity-details">
                  <div class="activity-title"><%= download.status === 'success' ? 'Successful download' : 'Failed download' %></div>
                  <div class="activity-meta">
                    <span>Session: <%= download.sessionId %></span>
                    <span class="activity-time"><i class="far fa-clock"></i> <%= download.timestamp.toLocaleTimeString() %></span>
                  </div>
                </div>
              </li>
            <% }); %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</body>
</html>