<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Profile | Contact Gain</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #6366f1;
      --primary-light: #818cf8;
      --secondary: #10b981;
      --danger: #ef4444;
      --warning: #f59e0b;
      --info: #0ea5e9;
      --dark: #1e293b;
      --light: #f8fafc;
      --gray: #94a3b8;
      --card-bg: #ffffff;
      --card-shadow: 0 4px 15px rgba(0,0,0,0.1);
    }
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Inter', sans-serif;
    }
    body {
      background: linear-gradient(to bottom, #e0e7ff, #f1f5f9);
      color: #334155;
      min-height: 100vh;
      padding: 2rem;
      animation: fadeIn 1s ease-in;
    }
    @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    .profile-container {
      max-width: 800px;
      margin: 0 auto;
      background: var(--card-bg);
      border-radius: 16px;
      overflow: hidden;
      box-shadow: var(--card-shadow);
      animation: slideInUp 0.7s ease-out;
    }
    @keyframes slideInUp { from { transform: translateY(50px); opacity: 0; } to { transform: translateY(0); opacity: 1; } }
    .profile-header {
      background: linear-gradient(135deg, var(--primary), var(--primary-light));
      padding: 3rem 2rem 2rem;
      text-align: center;
      position: relative;
    }
    .profile-avatar {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      background: white;
      margin: 0 auto 1.5rem;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 3rem;
      color: var(--primary);
      position: relative;
      border: 5px solid white;
      transition: transform 0.3s ease;
    }
    .profile-avatar:hover { transform: scale(1.1); }
    .profile-avatar img {
      width: 100%;
      height: 100%;
      border-radius: 50%;
      object-fit: cover;
    }
    .online-status {
      position: absolute;
      bottom: 10px;
      right: 10px;
      width: 20px;
      height: 20px;
      background: var(--secondary);
      border-radius: 50%;
      border: 3px solid white;
    }
    .profile-name {
      font-size: 1.8rem;
      font-weight: 700;
      color: white;
      margin-bottom: 0.5rem;
    }
    .profile-username {
      font-size: 1.2rem;
      color: rgba(255,255,255,0.8);
      margin-bottom: 1rem;
    }
    .admin-badge {
      display: inline-block;
      background: var(--info);
      color: white;
      padding: 0.3rem 0.8rem;
      border-radius: 20px;
      font-size: 0.9rem;
      font-weight: 500;
      margin-bottom: 1.5rem;
    }
    .profile-actions {
      display: flex;
      justify-content: center;
      gap: 1rem;
      margin-top: 1.5rem;
    }
    .profile-btn {
      padding: 0.8rem 1.5rem;
      border-radius: 30px;
      font-weight: 500;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    .profile-btn:hover { transform: scale(1.05); }
    .message-btn {
      background: white;
      color: var(--primary);
      border: none;
    }
    .message-btn:hover { background: rgba(255,255,255,0.9); }
    .edit-btn, .more-btn {
      background: rgba(255,255,255,0.2);
      color: white;
      border: none;
    }
    .edit-btn:hover, .more-btn:hover { background: rgba(255,255,255,0.3); }
    .profile-body {
      padding: 2rem;
    }
    .section-title {
      font-size: 1.3rem;
      font-weight: 600;
      margin-bottom: 1.5rem;
      padding-bottom: 0.5rem;
      border-bottom: 1px solid #e2e8f0;
    }
    .profile-details {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1.5rem;
      margin-bottom: 2rem;
    }
    .detail-item {
      background: #f8fafc;
      border-radius: 12px;
      padding: 1.5rem;
      box-shadow: var(--card-shadow);
      transition: transform 0.3s ease;
    }
    .detail-item:hover { transform: translateY(-5px); }
    .detail-label {
      font-size: 0.9rem;
      color: var(--gray);
      margin-bottom: 0.5rem;
    }
    .detail-value {
      font-size: 1.1rem;
      font-weight: 500;
    }
    .bio-content {
      background: #f8fafc;
      border-radius: 12px;
      padding: 1.5rem;
      line-height: 1.6;
      box-shadow: var(--card-shadow);
    }
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 1rem;
      margin-top: 2rem;
    }
    .stat-card {
      background: #f8fafc;
      border-radius: 12px;
      padding: 1.5rem;
      text-align: center;
      box-shadow: var(--card-shadow);
      transition: transform 0.3s ease;
    }
    .stat-card:hover { transform: translateY(-5px); }
    .stat-value {
      font-size: 1.8rem;
      font-weight: 700;
      color: var(--primary);
      margin-bottom: 0.5rem;
    }
    .stat-label {
      font-size: 0.9rem;
      color: var(--gray);
    }
    @media (max-width: 768px) {
      .profile-header { padding: 2rem 1rem 1.5rem; }
      .profile-avatar { width: 100px; height: 100px; font-size: 2.5rem; }
      .profile-name { font-size: 1.5rem; }
      .profile-username { font-size: 1rem; }
    }
  </style>
</head>
<body>
  <div class="profile-container">
    <div class="profile-header">
      <div class="profile-avatar">
        <% if (profileUser.profile && profileUser.profile.profilePic) { %>
          <img src="<%= profileUser.profile.profilePic %>" alt="Profile">
        <% } else { %>
          <%= profileUser.username.charAt(0).toUpperCase() %>
        <% } %>
        <% if (profileUser.lastSeen > Date.now() - 300000) { %>
          <div class="online-status"></div>
        <% } %>
      </div>
      <div class="profile-name">
        <%= profileUser.profile?.name || profileUser.username %>
        <% if (profileUser.isAdmin) { %>
          <span class="admin-badge"><i class="fas fa-check-circle"></i> Admin</span>
        <% } %>
      </div>
      <div class="profile-username">@<%= profileUser.username %></div>
      <div class="profile-actions">
        <div class="profile-btn message-btn" id="messageBtn"><i class="fas fa-envelope"></i> Message</div>
        <% if (currentUser._id.toString() === profileUser._id.toString()) { %>
          <div class="profile-btn edit-btn" id="editProfileBtn"><i class="fas fa-edit"></i> Edit Profile</div>
        <% } else if (currentUser.isAdmin) { %>
          <div class="profile-btn more-btn" id="adminActions"><i class="fas fa-cog"></i> Admin Actions</div>
        <% } %>
      </div>
    </div>

    <div class="profile-body">
      <div class="section-title">Profile Information</div>
      <div class="profile-details">
        <div class="detail-item">
          <div class="detail-label">Full Name</div>
          <div class="detail-value"><%= profileUser.profile?.name || 'Not provided' %></div>
        </div>
        <div class="detail-item">
          <div class="detail-label">Phone Number</div>
          <div class="detail-value"><%= profileUser.profile?.phone || 'Not provided' %></div>
        </div>
        <div class="detail-item">
          <div class="detail-label">Status</div>
          <div class="detail-value">
            <span class="<%= profileUser.status === 'active' ? 'text-green-600' : profileUser.status === 'suspended' ? 'text-yellow-600' : 'text-red-600' %>">
              <%= profileUser.status.charAt(0).toUpperCase() + profileUser.status.slice(1) %>
            </span>
          </div>
        </div>
        <div class="detail-item">
          <div class="detail-label">Member Since</div>
          <div class="detail-value"><%= new Date(profileUser.createdAt).toLocaleDateString() %></div>
        </div>
      </div>

      <div class="section-title">Bio</div>
      <div class="bio-content"><%= profileUser.profile?.bio || 'No bio provided' %></div>

      <div class="section-title">Statistics</div>
      <div class="stats-grid">
        <div class="stat-card">
          <div class="stat-value"><%= (await Session.countDocuments({ userId: profileUser._id })) || 0 %></div>
          <div class="stat-label">Sessions</div>
        </div>
        <div class="stat-card">
          <div class="stat-value"><%= (await Contact.countDocuments({ sessionId: { $in: (await Session.find({ userId: profileUser._id })).map(s => s.sessionId) } })) || 0 %></div>
          <div class="stat-label">Contacts</div>
        </div>
        <div class="stat-card">
          <div class="stat-value"><%= (await Download.countDocuments({ sessionId: { $in: (await Session.find({ userId: profileUser._id })).map(s => s.sessionId) } })) || 0 %></div>
          <div class="stat-label">Downloads</div>
        </div>
        <div class="stat-card">
          <div class="stat-value"><%= (await Message.countDocuments({ userId: profileUser._id })) || 0 %></div>
          <div class="stat-label">Messages</div>
        </div>
      </div>
    </div>
  </div>

  <script>
    document.getElementById('messageBtn').addEventListener('click', () => {
      window.location.href = `/private-chat/<%= profileUser._id %>`;
    });
    <% if (currentUser._id.toString() === profileUser._id.toString()) { %>
      document.getElementById('editProfileBtn').addEventListener('click', () => {
        window.location.href = '/edit-profile';
      });
    <% } %>
  </script>
</body>
</html>
