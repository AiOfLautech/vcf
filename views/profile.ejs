<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile | Contact Gain</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #6366f1;
            --primary-light: #818cf8;
            --dark: #1e293b;
            --light: #f8fafc;
            --gray: #94a3b8;
            --card-bg: #ffffff;
            --card-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }
        body {
            background-color: #f1f5f9;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        .profile-container {
            background: var(--card-bg);
            border-radius: 16px;
            padding: 2rem;
            box-shadow: var(--card-shadow);
            width: 100%;
            max-width: 500px;
        }
        .profile-pic {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            margin: 0 auto 1rem;
            display: block;
        }
        .username {
            font-size: 1.8rem;
            font-weight: 700;
            text-align: center;
            color: var(--dark);
            margin-bottom: 0.5rem;
        }
        .verified {
            color: #10b981;
        }
        .info {
            margin: 1rem 0;
            color: var(--dark);
        }
        .info label {
            font-weight: 500;
            margin-right: 0.5rem;
        }
        .edit-form {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }
        .input-group {
            display: flex;
            flex-direction: column;
        }
        .input-group label {
            margin-bottom: 0.5rem;
            font-weight: 500;
        }
        .input-group input, .input-group textarea {
            padding: 0.8rem;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            font-size: 1rem;
        }
        .action-btn {
            padding: 0.8rem;
            background: var(--primary);
            border: none;
            border-radius: 8px;
            color: white;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-align: center;
            text-decoration: none;
        }
        .action-btn:hover {
            background: var(--primary-light);
        }
        @media (max-width: 576px) {
            .profile-container {
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="profile-container">
        <% if (isEdit) { %>
            <form class="edit-form" action="/profile/edit" method="POST" enctype="multipart/form-data">
                <img src="<%= profileUser.profilePicture %>" class="profile-pic" alt="<%= profileUser.username %>">
                <div class="input-group">
                    <label for="profilePicture">Profile Picture</label>
                    <input type="file" id="profilePicture" name="profilePicture" accept="image/*">
                </div>
                <div class="input-group">
                    <label for="bio">Bio</label>
                    <textarea id="bio" name="bio" rows="3"><%= profileUser.bio %></textarea>
                </div>
                <div class="input-group">
                    <label for="phone">Phone</label>
                    <input type="text" id="phone" name="phone" value="<%= profileUser.phone %>">
                </div>
                <button type="submit" class="action-btn">Save Changes</button>
            </form>
        <% } else { %>
            <img src="<%= profileUser.profilePicture %>" class="profile-pic" alt="<%= profileUser.username %>">
            <h1 class="username"><%= profileUser.username %> <%= profileUser.isAdmin ? '<i class="fas fa-check-circle verified"></i>' : '' %></h1>
            <div class="info"><label>Phone:</label> <%= profileUser.phone || 'Not set' %></div>
            <div class="info"><label>Bio:</label> <%= profileUser.bio || 'No bio yet' %></div>
            <% if (profileUser._id.toString() === user._id.toString()) { %>
                <a href="/profile/edit" class="action-btn">Edit Profile</a>
            <% } else { %>
                <a href="/private-chat/<%= profileUser._id %>" class="action-btn">Message</a>
            <% } %>
            <a href="/chat" class="action-btn" style="margin-top: 1rem;">Back to Chat</a>
        <% } %>
    </div>
</body>
</html>
